{
  "name": "byreixwift-root",
  "private": true,
  "scripts": {
    "dev:client": "envrepair doctor -e client/.env.local -x client/.env.example && pnpm --filter byreixwift dev",
    "dev:server": "envrepair doctor -e server/.env -x server/.env.example && pnpm --filter byreixwift-server dev",
    "dev:all": "pnpm dev:client & pnpm dev:server",
    "fix": "pnpm -r fix",
    "lint": "pnpm -r lint",
    "build": "pnpm -r build"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "@nestjs/core",
      "esbuild",
      "unrs-resolver",
      "keccak"
    ]
  },
  "devDependencies": {
    "envrepair": "^0.2.1"
  }
}
